# Core Django & ASGI / Channels
Django>=5.0,<6.1
asgiref>=3.8.0
sqlparse>=0.5.0
tzdata>=2024.1

# Daphne ASGI Web Server (Required for WebSockets & HTTP)
daphne>=4.1.0
Twisted>=24.0.0
autobahn>=24.0.0

# Django Channels & Redis
channels>=4.1.0
channels_redis>=4.2.0
redis>=5.0.0

# REST Framework & CORS
djangorestframework>=3.15.0
django-cors-headers>=4.3.0

# Production Static Files & WSGI/ASGI Fallback
whitenoise>=6.6.0
gunicorn>=22.0.0

# Utilities
ujson>=5.9.0
cryptography>=42.0.0
service-identity>=24.1.0
